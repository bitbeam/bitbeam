// Bitbeam constants.
// All metric mm.


// Base measure (LEGO standard)
base = 0.8;

// Hole width
hw = 6 * base;

// Hole spacing
hs = 10 * base;

// beam width
bw = 10 * base;

// Beam height
bh = bw;

// Plate height
ph = bh/2;

// Quality of shapes?
$fn=25;
